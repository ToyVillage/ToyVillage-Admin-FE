---
feature: loading-skeleton-detail
figma:
  fileKey: P7Jhnu8qV5m9q2QJNzkwAN
  nodeId: 305:12757
  relatedNodeIds:
    - 2238:19936
    - 2238:19977
    - 2238:19991
    - 2238:20031
    - 2238:20192
    - 2238:20288
    - 2238:20340
    - 2238:20430
    - 2238:20550
    - 2238:20683
    - 2238:20804
    - 2238:20833
    - 2238:21809
    - 2238:21879
    - 2238:21904
    - 2238:21937
    - 2238:21994
    - 2238:22176
    - 2238:22326
    - 2238:22397
    - 2238:22491
    - 2238:22555
requires_functional_test: true
paths: src/shared/ui, src/pages
---

# 로딩 스켈레톤 · 상세/수정 행동명세

## 목적

상세·수정 화면이 첫 조회 중일 때 "불러오는 중" 카드나 빈 화면 대신 Figma `스켈레톤 · 상세`(`2021:24414`)·`스켈레톤 · 수정`(`2021:24415`) 모양의 스켈레톤을 보인다. 이슈 #108의 2차 범위이고, 2026-09-20 Figma 재작업분(`2238:*` 프레임)을 기준으로 한다. 공통 부품은 `loading-skeleton-list`에서 만든 `Skeleton`·`SkeletonStatus`를 재사용한다.

## 동작 (source of truth)

- 화면의 첫 조회가 끝나기 전에는 해당 화면의 스켈레톤을 보인다.
  - 스켈레톤 루트는 `role="status"`, `aria-busy="true"`, 접근 이름 `불러오는 중`을 가진다(`SkeletonStatus`).
- **서버 데이터가 아닌 UI는 조회 중에도 실제 UI 그대로 보인다.** 조회 결과로 채워지는 자리만 막대로 대체한다.
  - 실제로 보이는 것: `뒤로가기` 링크, 카드 섹션 라벨(`분류`·`날짜`·`첨부자료` 등 고정 필드명), 폼 라벨과 필수 표시, 첨부 파일 유형 아이콘·다운로드 아이콘, 고정 버튼 라벨.
  - 막대로 대체하는 것: 서버가 주는 값 — 필드 값, 제목·본문, 첨부 파일명, 폼 입력에 들어갈 값.
- 조회가 성공하면 막대가 사라지고 실제 내용(상세) 또는 값이 채워진 폼(수정)이 보인다.
- 조회가 실패하거나 대상이 없으면 스켈레톤이 사라지고 기존 오류·없음 표시를 그대로 보인다.
- 수정 화면은 조회 중에 입력할 수 있는 빈 폼을 보이지 않는다(값이 채워지기 전 입력 방지).
- 적용 화면과 Figma 프레임:

  | 화면 | 경로 | Figma |
  | --- | --- | --- |
  | 공지사항 상세 | `/notices/list/:id` | `2238:19936` |
  | 휴관일 상세 | `/notices/guide/:id` | `2238:19977` |
  | 업무 상세 | `/tasks/:id` | `2238:20192` |
  | 업무보고 상세 | `/task-reports/:id` | `2238:20288` |
  | 업무일지 상세 | `/work-logs/:id` | `2238:20340` |
  | 업무일지 양식 상세 | `/work-logs/forms/:id` | `2238:20430` |
  | 먹이 급여 상세 | `/feeds/:id` | `2238:20833` |
  | 자료실 상세 | `/notices/resources/:id` | `2238:19991` |
  | 종 상세 | `/species/:speciesId` | `2238:20550` |
  | 개체 상세 | `/species/:speciesId/individuals/:individualId` | `2238:20683` |
  | 관찰 상세 | `/species/.../observations/:observationId` | `2238:20804` |
  | 공지사항 수정 | `/notices/list/:id/edit` | `2238:21809` |
  | 휴관일 수정 | `/notices/guide/:id/edit` | `2238:21879` |
  | 운영시간 수정 | `/notices/guide/hours/:date` | `2238:21904` |
  | 자료실 수정 | `/notices/resources/:id/edit` | `2238:21937` |
  | 단체예약 수정 | `/notices/reservations/:id` | `2238:21994` (상세 `2238:20031` 동일 화면) |
  | 종 수정 | `/species/:speciesId/edit` | `2238:22397` |
  | 개체 수정 | `/species/:speciesId/individuals/:individualId/edit` | `2238:22491` |
  | 관찰 수정 | `/species/.../observations/:observationId/edit` | `2238:22555` |

## 데이터

- 기존 TanStack Query 조회의 `isPending`만 사용한다. 새 API 연결 없음.

## 컴포넌트 구조/props

- 화면별 스켈레톤은 각 page(`ui/`)에 둔다. 실제 화면 컴포넌트(카드·라벨·뒤로가기)를 그대로 쓰고 값 자리에만 막대를 넣는다.

## 비고

- 단체예약은 상세 경로가 곧 수정 폼이라 `수정` 프레임을 기준으로 구현한다.
- 자료실은 #92 에서 읽기 전용 상세(`/notices/resources/:id`)와 수정(`…/edit`)이 갈렸다. 상세는 `2238:19991`, 수정은 `2238:21937` 을 따른다(#180).
- `업무일지 양식 수정`(`2238:22176`)·`업무일지 구역 수정`(`2238:22326`)은 조회하는 수정 화면이 없어 제외한다.
- 업무 수정(`/tasks/:id/edit`)은 Figma 스켈레톤 프레임이 없어 이번 범위에서 제외한다.
- 막대 쉬머 애니메이션은 `loading-skeleton-list` 비고와 같다(이슈 #173).
